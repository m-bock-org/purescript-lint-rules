name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        # spago 1.0.4 reads its package index through node:sqlite
        node-version: '24'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build
      run: npx spago build --strict


    - name: README is in sync with the rules
      run: |
        cp README.md /tmp/README.before
        npx spago test -m Test.Docs
        diff -u /tmp/README.before README.md

    - name: Lint
      run: npx spago test -m Test.Lint
